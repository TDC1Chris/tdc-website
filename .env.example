# Environment configuration for auto_translate_site.py
# Copy this to .env and set your actual values

# REQUIRED: Your OpenAI API key
# Get this from: https://platform.openai.com/api-keys
# NEVER commit this file to git!
OPENAI_API_KEY=your_openai_api_key_here

# SAFETY: Maximum translations per run (prevents runaway costs)
# Default: 1000 (approximately $3-5 USD maximum cost)
# For full site translation, increase to 5000-10000
MAX_TRANSLATIONS_PER_RUN=1000

# SECURITY: Require user confirmation before running
# Set to "false" only for automated environments
REQUIRE_CONFIRMATION=true

# COST CONTROL: Alternative API settings
# Uncomment these lines to use different models or limits
# OPENAI_MODEL=gpt-4o-mini
# MAX_TOKENS_PER_REQUEST=500
# TRANSLATION_DELAY=0.5
